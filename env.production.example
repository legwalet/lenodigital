# Production Environment Variables for LenoDigital

# NextAuth.js URL for authentication callbacks
# IMPORTANT: Replace with your deployed Netlify URL (e.g., https://your-app-name.netlify.app)
NEXTAUTH_URL=https://your-app-name.netlify.app

# A secret string used to sign and encrypt the session cookie.
# Generate a strong, random string (e.g., using `openssl rand -base64 32`)
NEXTAUTH_SECRET=your-secure-random-string-here

# Database connection string for your production database
# Using Neon PostgreSQL database
DATABASE_URL=postgresql://neondb_owner:npg_bP0lKTQ1EUoR@ep-blue-dust-adpzyffd-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# The public URL of your application.
# IMPORTANT: Replace with your deployed Netlify URL (e.g., https://your-app-name.netlify.app)
APP_URL=https://your-app-name.netlify.app

# Email Configuration (optional for production)
EMAIL_SERVER_HOST=smtp.gmail.com
EMAIL_SERVER_PORT=587
EMAIL_SERVER_USER=your-email@gmail.com
EMAIL_SERVER_PASSWORD=your-app-password
EMAIL_FROM=noreply@lenodigital.com

# File Upload
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760

# SMS Configuration (optional)
SMS_API_KEY=your-sms-api-key
SMS_SENDER_ID=LenoDigital

# App Configuration
APP_NAME=LenoDigital